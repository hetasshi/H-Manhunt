# modded hetashi
# Конфигурация Minecraft Manhunt

# Если true, при старте игры будет установлено дневное время
timeSetDayOnStart: true

# Если true, при старте игры погода будет ясной
weatherClearOnStart: true

# Длительность форы для спидраннеров в секундах
headStartDuration: 0

# Количество жизней у спидраннеров
speedrunnersLives: 1

# Если true, погибшие игроки перейдут в режим наблюдателя
spectatorAfterDeath: true

# Если true, игроки будут телепортированы друг к другу при старте игры
teleport: true

# Если true, охотники смогут отслеживать спидраннеров через порталы
trackPortals: true

# Если true, включен огонь по своим (friendly fire)
friendlyFire: true

# Если true, охотники могут использовать меню для выбора цели отслеживания
compassMenu: false

# Если true, охотники могут выбрать отслеживание ближайшего спидраннера
trackNearestMode: true

# Если true, инвентари будут очищены при старте игры
clearInventories: true

# Если true, игроки потеряют статус оператора при старте и получат его обратно после завершения
takeAwayOps: true

# Если true, только игроки с правами 'manhunt.help', 'manhunt.rules' и т.д. смогут использовать команды
usePermissions: false

# Если true, используется новый BossBar радар. Если false - стандартная подсветка (Locator Bar v1.21)
useBossBarRadar: false

# Если true, команды /manhunt pause и /manhunt unpause доступны
enablePauses: true

# Если true, во время активной игры печки плавят в 2 раза быстрее
fastplavka: true

# --- Матч-миры / собственная автогенерация ---
# Если true, /manhunt start будет автоматически создавать новый мир под матч
matchWorlds:
  enabled: true
  autoGenerate:
    # Префикс имени мира. Каждый новый матч получает уникальное имя
    worldPrefix: "manhunt_match_"
    # Сколько последних автосозданных миров хранить загруженными/на диске
    keepLatestWorlds: 1
    # Сколько сидов пробовать перед выбором лучшего кандидата
    maxAttempts: 2
    # Минимальный скор мира, после которого он принимается сразу
    minScoreToAccept: 90
    # Если идеальный мир не найден, взять лучший из проверенных
    acceptBestCandidateIfThresholdMissed: true
    # Можно заранее указать список сидов. Если пусто - сиды случайные
    fixedSeeds: []
    # Радиус поиска полезных структур вокруг спавна в чанках
    structureSearchRadiusChunks: 18
    # На каком расстоянии от ключевой структуры ставить стартовую точку
    startDistanceFromAnchor: 100

# --- Казуальный режим ---
# Включает способности охотников через компас (Shift + ПКМ)
casual: true

# Способность Shadows Warp
warpShadowsCooldown: 300
warpShadowsMaxDistance: 200
warpShadowsBufferZone: 40

# --- Автообновление плагина через GitHub Releases ---
# Настройки команд: /manhunt update check и /manhunt update download
update:
  # true - система обновлений включена, false - полностью отключена
  enabled: true
  # true - проверять новую версию при запуске сервера
  checkOnStartup: true
  # Репозиторий с релизами в формате owner/repo
  owner: "hetasshi"
  repository: "H-Manhunt"
  # false - только обычные релизы, true - также prerelease
  allowPrerelease: false
  # Имя .jar файла из релиза для скачивания (точное совпадение, без учета регистра)
  # Пример: "H-Manhunt-2.4.1.jar"
  # Если оставить пустым, будет выбран первый .jar asset из релиза
  # Советую просто не трогать это
  assetName: ""
